# Anthropic API key for the single-stock tool's LLM agents (news /
# announcements / decision synthesis). Only required when
# singlestock.llm_enabled is true in config/settings.yaml.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the config file path
# ALGOTRADER_CONFIG=config/settings.yaml
